<!DOCTYPE html>
<html>
	<head> 
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
		<meta charset="UTC-8">
		<link rel="stylesheet" type="text/css" href="../css/main.css">
	</head>
	<body>
		<div class='top'>
			<div class='title'>
				<a href="/welcome"><strong>thanx</strong>MATZ</a>
			</div>

			<div class='welcome'>
				<a href="/profile"><%= @user.username %></a>
			</div>
		
			<form class='logout' action="/log-out">
				<input type='submit' value='Log out'>
			</form>
		</div>

		<div class='content'>
			<h1 class="othersprofile"><%= @temp.username %>'s Profile</h1>
			
			<div class='otherstable'>
				<table>
					<tr>
						<td>Real Name</td>
						<td><strong><%= @profile.fname << " " << @profile.lname %></strong></td>
					</tr>
					<tr>
						<td>Email Address</td>
						<td><strong><%= @profile.email %></strong></td>
					</tr>
					<tr>
						<td>Home Town</td>
						<td><strong><%= @profile.city %></strong></td>
					</tr>
				</table>
			</div>


			<div class='othersfeed'>
			<h1><%= @temp.username %>'s Posts</h1>
		<div class='othersposts'>
				<% @blogs.reverse.each do |blog| %>
			<div><%= blog.user.username unless blog.user.nil? %><span><%= blog.created_at %></span></div>
			<p><%= blog.content %></p>
				<% end %>
		</div>

		</div>
	</body>
</html>